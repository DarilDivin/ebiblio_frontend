// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ['localhost'],
//   },
// };

// export default nextConfig;


const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost'], // Remplacez par le domaine de votre API Laravel
  },
};

export default nextConfig;